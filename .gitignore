.venv/
__pycache__/
*.pyc
.pytest_cache/
.claude/
reviews.json
server.log
.DS_Store

# Real secrets live here and must never be committed. .env.example is the
# tracked template; .env is yours and stays local.
.env
.env.*
!.env.example
